# smoke-test settings
model_name = "Qwen/Qwen2.5-1.5B-Instruct"
output_dir = "outputs/Qwen-1.5B-MLX-GRPO"
run_name   = "Qwen-1.5B-MLX-GRPO-gsm8k-test"

# training
learning_rate = 1e-6
num_epochs = 1
batch_size = 1
gradient_accumulation_steps = 4
warmup_ratio = 0.1
max_grad_norm = 0.1
logging_steps = 1

# sampling / rollout
num_generations = 8
max_new_tokens = 128
temperature = 0.7
clip_eps = 0.2
kl_coeff = 0.0

# eval & logging
eval_steps = 50
eval_samples = 200
eval_every_updates = 25
eval_subset_size = 200
eval_max_new_tokens = 128
log_jsonl = true

# system
seed = 0
use_compile = true

